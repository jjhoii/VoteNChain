export const abi = require("../build/contracts/VoteManager.json").abi;

export const contractAddress = process.env.CONTRACT_ADDRESS;

export const gethHost = process.env.BLOCKCHAIN_PROVIDER;

export const coinbase = process.env.COINBASE;
